-- ---------------------------------------------------------------------------
-- 回に「どこから始めるか」の自由記述を持たせる
--
-- 数値の page_from / page_to と役割を分けている。
-- 数値は「p.71〜95」のような機械的な範囲で、進み具合を機械が扱うための値。
-- こちらは「p.27の章末2.3から」のように、人が読んで場所が分かる説明。
--
-- 章・節・演習番号で開始位置を伝えたい場面はページ番号だけでは表せず、
-- かといって数値の列に文字列を入れると進み具合の計算が壊れる。
-- どちらかに寄せず両方持たせる（2026-08-12に決定、Issue #38）。
--
-- 更新ポリシーは既存の "editors can update units" がそのまま及ぶ
-- （deleted_at 以外は編集者なら誰でも変更できる設計のため、列を足すだけでよい）。
-- ---------------------------------------------------------------------------

alter table public.units
  add column start_note text;
