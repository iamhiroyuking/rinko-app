# 設計上の未決事項リスト

初版の約60問は2026-08-10に全て決着し、[requirements.md](requirements.md) と
[screen-flow.md](screen-flow.md) に反映済み。設問と回答の原文はObsidianの
`04.Library/02_Tech/20260810_輪講アプリ_設計上の未決事項` にある。

**データの持ち方に影響する論点はすべて決着した。** スキーマを適用できる状態にある。

---

## 解決済み

| 論点 | 結論 |
|---|---|
| 課題「全員が理解できているか分からない」への対応 | 運用してから判断する。まずは返信で質問できる形で使う |
| 全員が教材を完全削除した後のデータ | データベースから削除する（`delete_orphan_book()` として実装） |
| リアルタイム更新の方式 | 画面遷移時に再取得し、新着があれば通知して手動更新を促す |
| 本棚のステータスの置き場所 | **`Book` ではなく `Membership` へ移した。** 教材側に置くと、誰かが「学習済み」にした瞬間に共有相手全員のホームから消えてしまう |
| 招待リンクで参加できない問題 | リンクを受け取った人はまだ参加者ではなく `invite_links` を読めないため照合できなかった。`join_book_with_token()` を用意して解決 |
| 教材の作成者が自分の教材を見られない問題 | 閲覧の条件が「参加していること」なので、作成時に参加情報を作る必要があった。`handle_new_book()` として実装 |
| 新着通知に必要な既読状態 | `Membership.last_seen_at` を追加 |
| ログのマークは個人か共有か | **個人のしおり。** 他人のマークは見えない。`log_marks(log_id, user_id)` として別テーブルに切り出した |
| 回を削除したとき誰のゴミ箱に入るか | **作成者のゴミ箱だけ。** 追加と編集は参加者全員で同期し、削除だけ作った本人に限る。削除すると全員の画面から消えるが、復元できるのは作った人だけ |

### 共有されているものの扱い（原則）

| 操作 | 挙動 |
|---|---|
| 追加 | 参加者全員に同期する |
| 編集 | 参加者全員に同期する |
| 削除 | 作った本人だけができる |

ログの「自分の投稿のみ編集・削除できる」と同じ考え方。
実装上は、回の編集自体は編集者全員に許しつつ、`deleted_at` の変更だけを
`protect_unit_deletion()` トリガーで作成者に限定している
（行レベルセキュリティのポリシーは「どの列が変わったか」を区別できないため）。

---

## 未決（実装時に判断すれば足りる）

詳細は [features.md の「要検討」](features.md#要検討)。

| # | 論点 | 推奨 |
|---|---|---|
| Q3 | 添付ファイルの保護方針 | 非公開バケット＋期限付きURL。**放置すると研究内容が漏れる** |
| Q4 | 第N回の番号が重複したときの並び順 | 作成日時が古い順 |

---

## 実装しながら決めるもの

- **デザインルール**（色・余白・ボタンの形）— ある程度作ってから精査する方針
- **ログの種別の値** — `none / preview / question / review` で始めて、使いながら追加する
- ~~**表紙画像の入手方法** — アップロードか、URLを貼るか~~
  → **両方（2026-08-15に決着）。** 手元の画像を選べるようにし、URLも残した。
  画像を選んだ場合はそちらを優先する。保存先はログの画像と同じバケットの
  `<book_id>/cover/` で、教材を完全削除したときの後片付けに自動で含まれる
